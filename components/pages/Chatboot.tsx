import Layout from "./Layout";
import ChatbootContainer from "../ChatbootContainer";
const Chatboot = () => {
  return (
    <Layout>
      <ChatbootContainer />
    </Layout>
  );
};

export default Chatboot;
