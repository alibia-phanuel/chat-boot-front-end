import Layout from "./Layout";
import MessageContainer from "../MessageContainer";

const Message = () => {
  return (
    <Layout>
      <MessageContainer />
    </Layout>
  );
};

export default Message;
